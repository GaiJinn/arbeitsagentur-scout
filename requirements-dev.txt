-r requirements.txt
pytest>=8.0,<9.0
respx>=0.21,<1.0
# dashboard.py imports pandas unconditionally (used for filtering/sorting the
# job history); only needed here so tests/test_dashboard.py can run in CI.
# streamlit itself is NOT a test dependency — it's only imported inside
# dashboard.py's _render(), which only runs under `streamlit run`. See
# requirements-dashboard.txt for that.
pandas>=2.0,<3.0
